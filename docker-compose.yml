version: "3.8"

services:
  leyu-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${DOCKER_IMAGE_NAME:-leyu-frontend}:${DOCKER_IMAGE_TAG:-latest}
    container_name: ${CONTAINER_NAME:-leyu-frontend-app}
    ports:
      - "${HOST_PORT:-3000}:3000"
    environment:
      # API Configuration
      - NEXT_PUBLIC_API_BASE_URL=${NEXT_PUBLIC_API_BASE_URL:-http://localhost:3001/api}
      - NEXT_PUBLIC_BASE_URL=${NEXT_PUBLIC_BASE_URL:-http://localhost:3000}
      
      # Authentication Configuration
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET:-your-strong-secret-here}
      - NEXTAUTH_URL=${NEXTAUTH_URL:-http://localhost:3000}
      
      # Optional Configuration
      - NODE_ENV=${NODE_ENV:-production}
    restart: unless-stopped
    networks:
      - leyu-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  leyu-network:
    driver: bridge

# To use this docker-compose file:
# 1. Copy .env.example to .env and configure your environment variables
# 2. Run: docker-compose up -d
# 3. Access the application at http://localhost:3000 (or your configured HOST_PORT)